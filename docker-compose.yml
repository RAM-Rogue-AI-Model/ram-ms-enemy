services:
  ram-ms-enemy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ram-ms-enemy
    restart: unless-stopped
    ports:
      - '3004:3004'
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    networks:
      - ram-shared-network

  db:
    image: mariadb:latest
    container_name: ram-ms-enemy-db
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: ${DATABASE_PASSWORD:-ram_password}
      MARIADB_DATABASE: ${DATABASE_NAME:-ram-ms-enemy-db}
      MARIADB_USER: ${DATABASE_USER:-user}
      MARIADB_PASSWORD: ${DATABASE_PASSWORD:-ram_password}
    ports:
      - '3304:3306'
    volumes:
      - mariadb_data:/var/lib/mysql
      - ./dataset.sql:/docker-entrypoint-initdb.d/dataset.sql:ro

    networks:
      - ram-shared-network
    healthcheck:
      test: ['CMD', 'healthcheck.sh', '--connect', '--innodb_initialized']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

networks:
  ram-shared-network:
    external: true

volumes:
  mariadb_data:
